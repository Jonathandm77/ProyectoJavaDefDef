package principal.persistencia;

import java.text.ParseException;

import java.util.Date;

import principal.modelo.Alumno;
import principal.modelo.Coche;
import principal.modelo.Profesor;
import principal.modelo.Rol;
import principal.modelo.Usuario;

public class Tablas {

	/*
	 * public void crearTablas() throws ParseException {
	 * 
	 * Alumno a1 = new Alumno("Alba", "541245F"); Profesor p1 = new
	 * Profesor("Pedro", "4529L"); Coche c1 = new Coche("5894 GHL", "2023",
	 * "Peugeot"); Rol rolBasico = new Rol("ROLE_USER"); Rol rolAdmin = new
	 * Rol("ROLE_ADMIN"); Usuario usuarioAdmin = new Usuario("admin", "admin",
	 * "$2a$12$jcd/P/f86nT9YXQbx/3tJeQye8h0aMhNm6EzzKkZMNLUCtIxpTNJa");
	 * usuarioAdmin.getRoles().add(rolAdmin);
	 * 
	 * 
	 * LocalDate fecha=LocalDate.of(2024, 5, 13); c1.setFechaITV(fecha);
	 * 
	 * Date fecha = new Date(125, 6, 23); c1.setFechaITV(fecha);
	 * 
	 * a1.setProfesor(p1); p1.getAlumnos().add(a1); a1.setCoche(c1);
	 * c1.getAlumnos().add(a1); p1.getCoches().add(c1); c1.getProfesores().add(p1);
	 * 
	 * RolDAO rolDAO = new RolDAO(); UsuarioDAO usuarioDAO = new UsuarioDAO();
	 * AlumnoDAO alumnoDAO = new AlumnoDAO(); ProfesorDAO profesorDAO = new
	 * ProfesorDAO(); CocheDAO cocheDAO = new CocheDAO();
	 * 
	 * usuarioDAO.insertarUsuarioJPA(usuarioAdmin); rolDAO.insertarRolJPA(rolAdmin);
	 * rolDAO.insertarRolJPA(rolBasico); profesorDAO.insertarProfesorJPA(p1);
	 * cocheDAO.insertarCocheJPA(c1); alumnoDAO.insertarAlumnoJPA(a1);
	 * 
	 * }
	 */
}
